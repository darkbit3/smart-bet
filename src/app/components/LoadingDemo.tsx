import React, { useState } from 'react';\nimport LoadingEffect, { LoadingPresets, useLoading } from './LoadingEffect';\n\nexport default function LoadingDemo() {\n  const [showFullScreen, setShowFullScreen] = useState(false);\n  const [showOverlay, setShowOverlay] = useState(false);\n  const { isLoading, startLoading, stopLoading, LoadingComponent } = useLoading();\n\n  const loadingTypes = [\n    { value: 'spinner', label: 'Spinner', description: 'Classic rotating spinner' },\n    { value: 'pulse', label: 'Pulse', description: 'Pulsing circle effect' },\n    { value: 'bounce', label: 'Bounce', description: 'Three bouncing dots' },\n    { value: 'dots', label: 'Dots', description: 'Three pulsing dots' },\n    { value: 'wave', label: 'Wave', description: 'Five bars in wave pattern' },\n    { value: 'bars', label: 'Bars', description: 'Four vertical bars' },\n    { value: 'orbit', label: 'Orbit', description: 'Rotating circle with ping' },\n    { value: 'morph', label: 'Morph', description: 'Morphing shapes effect' }\n  ];\n\n  const colors = [\n    { value: 'primary', label: 'Primary', class: 'text-blue-500' },\n    { value: 'secondary', label: 'Secondary', class: 'text-gray-500' },\n    { value: 'success', label: 'Success', class: 'text-green-500' },\n    { value: 'warning', label: 'Warning', class: 'text-yellow-500' },\n    { value: 'error', label: 'Error', class: 'text-red-500' },\n    { value: 'info', label: 'Info', class: 'text-cyan-500' }\n  ];\n\n  const sizes = [\n    { value: 'sm', label: 'Small', size: 'w-4 h-4' },\n    { value: 'md', label: 'Medium', size: 'w-6 h-6' },\n    { value: 'lg', label: 'Large', size: 'w-8 h-8' },\n    { value: 'xl', label: 'Extra Large', size: 'w-12 h-12' }\n  ];\n\n  const presets = [\n    { key: 'gameLoading', label: 'Game Loading', description: 'Full screen game loading' },\n    { key: 'betProcessing', label: 'Bet Processing', description: 'Overlay bet processing' },\n    { key: 'authenticating', label: 'Authenticating', description: 'Full screen authentication' },\n    { key: 'dataFetching', label: 'Data Fetching', description: 'Simple data loading' },\n    { key: 'networkRequest', label: 'Network Request', description: 'Network connection' },\n    { key: 'simpleSpinner', label: 'Simple Spinner', description: 'Basic spinner only' },\n    { key: 'minimalDots', label: 'Minimal Dots', description: 'Minimal dots only' }\n  ];\n\n  return (\n    <div className=\"min-h-screen bg-gray-900 text-white p-8\">\n      <div className=\"max-w-6xl mx-auto\">\n        <h1 className=\"text-4xl font-bold mb-2 bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent\">\n          Loading Effects Demo\n        </h1>\n        <p className=\"text-gray-400 mb-8\">Explore all loading animations and configurations</p>\n\n        {/* Full Screen Demo */}\n        <div className=\"mb-8\">\n          <button\n            onClick={() => setShowFullScreen(true)}\n            className=\"bg-blue-600 hover:bg-blue-700 px-6 py-3 rounded-lg font-semibold transition-colors\"\n          >\n            Show Full Screen Loading\n          </button>\n        </div>\n\n        {/* Overlay Demo */}\n        <div className=\"mb-8\">\n          <button\n            onClick={() => setShowOverlay(true)}\n            className=\"bg-green-600 hover:bg-green-700 px-6 py-3 rounded-lg font-semibold transition-colors\"\n          >\n            Show Overlay Loading\n          </button>\n        </div>\n\n        {/* Hook Demo */}\n        <div className=\"mb-8\">\n          <button\n            onClick={() => {\n              startLoading('Processing with hook...', 'bounce');\n              setTimeout(stopLoading, 3000);\n            }}\n            className=\"bg-purple-600 hover:bg-purple-700 px-6 py-3 rounded-lg font-semibold transition-colors\"\n          >\n            Test Hook Loading\n          </button>\n        </div>\n\n        {/* Loading Types Grid */}\n        <div className=\"mb-12\">\n          <h2 className=\"text-2xl font-bold mb-6\">Loading Types</h2>\n          <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6\">\n            {loadingTypes.map((type) => (\n              <div key={type.value} className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n                <div className=\"flex justify-center mb-4\">\n                  <LoadingEffect type={type.value as any} size=\"lg\" />\n                </div>\n                <h3 className=\"text-lg font-semibold text-center mb-2\">{type.label}</h3>\n                <p className=\"text-sm text-gray-400 text-center\">{type.description}</p>\n              </div>\n            ))}\n          </div>\n        </div>\n\n        {/* Color Variations */}\n        <div className=\"mb-12\">\n          <h2 className=\"text-2xl font-bold mb-6\">Color Variations</h2>\n          <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6\">\n            {colors.map((color) => (\n              <div key={color.value} className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n                <div className=\"flex justify-center mb-4\">\n                  <LoadingEffect type=\"spinner\" size=\"lg\" color={color.value as any} />\n                </div>\n                <h3 className=\"text-lg font-semibold text-center mb-2\">{color.label}</h3>\n                <p className={`text-sm text-center ${color.class}`}>{color.value}</p>\n              </div>\n            ))}\n          </div>\n        </div>\n\n        {/* Size Variations */}\n        <div className=\"mb-12\">\n          <h2 className=\"text-2xl font-bold mb-6\">Size Variations</h2>\n          <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6\">\n            {sizes.map((size) => (\n              <div key={size.value} className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n                <div className=\"flex justify-center mb-4\">\n                  <LoadingEffect type=\"spinner\" size={size.value as any} />\n                </div>\n                <h3 className=\"text-lg font-semibold text-center mb-2\">{size.label}</h3>\n                <p className=\"text-sm text-gray-400 text-center\">{size.size}</p>\n              </div>\n            ))}\n          </div>\n        </div>\n\n        {/* Presets */}\n        <div className=\"mb-12\">\n          <h2 className=\"text-2xl font-bold mb-6\">Predefined Presets</h2>\n          <div className=\"space-y-4\">\n            {presets.map((preset) => {\n              const PresetComponent = LoadingPresets[preset.key as keyof typeof LoadingPresets] as any;\n              return (\n                <div key={preset.key} className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n                  <div className=\"flex items-center justify-between\">\n                    <div>\n                      <h3 className=\"text-lg font-semibold mb-1\">{preset.label}</h3>\n                      <p className=\"text-sm text-gray-400\">{preset.description}</p>\n                    </div>\n                    <div className=\"scale-75\">\n                      <PresetComponent />\n                    </div>\n                  </div>\n                </div>\n              );\n            })}\n          </div>\n        </div>\n\n        {/* Interactive Demo */}\n        <div className=\"mb-12\">\n          <h2 className=\"text-2xl font-bold mb-6\">Interactive Demo</h2>\n          <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n            <div className=\"grid grid-cols-1 md:grid-cols-3 gap-6\">\n              <div>\n                <label className=\"block text-sm font-medium mb-2\">Type</label>\n                <select className=\"w-full bg-gray-700 rounded px-3 py-2 text-white\">\n                  {loadingTypes.map(type => (\n                    <option key={type.value} value={type.value}>{type.label}</option>\n                  ))}\n                </select>\n              </div>\n              <div>\n                <label className=\"block text-sm font-medium mb-2\">Color</label>\n                <select className=\"w-full bg-gray-700 rounded px-3 py-2 text-white\">\n                  {colors.map(color => (\n                    <option key={color.value} value={color.value}>{color.label}</option>\n                  ))}\n                </select>\n              </div>\n              <div>\n                <label className=\"block text-sm font-medium mb-2\">Size</label>\n                <select className=\"w-full bg-gray-700 rounded px-3 py-2 text-white\">\n                  {sizes.map(size => (\n                    <option key={size.value} value={size.value}>{size.label}</option>\n                  ))}\n                </select>\n              </div>\n            </div>\n            <div className=\"mt-6 flex justify-center\">\n              <LoadingEffect type=\"spinner\" size=\"xl\" color=\"primary\" text=\"Custom Loading...\" />\n            </div>\n          </div>\n        </div>\n      </div>\n\n      {/* Full Screen Loading */}\n      {showFullScreen && (\n        <LoadingPresets.gameLoading\n          text=\"Full Screen Loading Demo...\"\n          onTimeout={() => setShowFullScreen(false)}\n        />\n      )}\n\n      {/* Overlay Loading */}\n      {showOverlay && (\n        <div className=\"relative\">\n          <div className=\"bg-gray-800 rounded-lg p-8 border border-gray-700\">\n            <h2 className=\"text-2xl font-bold mb-4\">Content Behind Overlay</h2>\n            <p className=\"text-gray-400\">This content is behind the loading overlay.</p>\n          </div>\n          <LoadingPresets.betProcessing\n            text=\"Overlay Loading Demo...\"\n            onTimeout={() => setShowOverlay(false)}\n          />\n        </div>\n      )}\n\n      {/* Hook Loading */}\n      <LoadingComponent fullScreen={isLoading} />\n    </div>\n  );\n}
