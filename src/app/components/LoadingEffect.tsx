import React from 'react';\nimport { Loader2, Spinner, RefreshCw, Activity, Zap, Circle } from 'lucide-react';\n\ninterface LoadingEffectProps {\n  type?: 'spinner' | 'pulse' | 'bounce' | 'dots' | 'wave' | 'bars' | 'orbit' | 'morph';\n  size?: 'sm' | 'md' | 'lg' | 'xl';\n  color?: 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'info';\n  text?: string;\n  showText?: boolean;\n  className?: string;\n  overlay?: boolean;\n  fullScreen?: boolean;\n}\n\nexport default function LoadingEffect({\n  type = 'spinner',\n  size = 'md',\n  color = 'primary',\n  text = 'Loading...',\n  showText = true,\n  className = '',\n  overlay = false,\n  fullScreen = false\n}: LoadingEffectProps) {\n  const getSizeClasses = () => {\n    switch (size) {\n      case 'sm': return 'w-4 h-4';\n      case 'md': return 'w-6 h-6';\n      case 'lg': return 'w-8 h-8';\n      case 'xl': return 'w-12 h-12';\n      default: return 'w-6 h-6';\n    }\n  };\n\n  const getColorClasses = () => {\n    switch (color) {\n      case 'primary': return 'text-blue-500';\n      case 'secondary': return 'text-gray-500';\n      case 'success': return 'text-green-500';\n      case 'warning': return 'text-yellow-500';\n      case 'error': return 'text-red-500';\n      case 'info': return 'text-cyan-500';\n      default: return 'text-blue-500';\n    }\n  };\n\n  const getTextSizeClasses = () => {\n    switch (size) {\n      case 'sm': return 'text-xs';\n      case 'md': return 'text-sm';\n      case 'lg': return 'text-base';\n      case 'xl': return 'text-lg';\n      default: return 'text-sm';\n    }\n  };\n\n  const renderLoadingIcon = () => {\n    const sizeClass = getSizeClasses();\n    const colorClass = getColorClasses();\n\n    switch (type) {\n      case 'spinner':\n        return <Loader2 className={`${sizeClass} ${colorClass} animate-spin`} />;\n      \n      case 'pulse':\n        return (\n          <div className={`${sizeClass} ${colorClass} animate-pulse`}>\n            <Circle className=\"w-full h-full\" fill=\"currentColor\" />\n          </div>\n        );\n      \n      case 'bounce':\n        return (\n          <div className=\"flex gap-1\">\n            {[0, 1, 2].map((i) => (\n              <div\n                key={i}\n                className={`${sizeClass.replace('w-6', 'w-2').replace('h-6', 'h-2').replace('w-8', 'w-3').replace('h-8', 'h-3').replace('w-12', 'w-4').replace('h-12', 'h-4')} ${colorClass} bg-current rounded-full animate-bounce`}\n                style={{ animationDelay: `${i * 0.1}s` }}\n              />\n            ))}\n          </div>\n        );\n      \n      case 'dots':\n        return (\n          <div className=\"flex gap-1\">\n            {[0, 1, 2].map((i) => (\n              <div\n                key={i}\n                className={`${sizeClass.replace('w-6', 'w-2').replace('h-6', 'h-2').replace('w-8', 'w-3').replace('h-8', 'h-3').replace('w-12', 'w-4').replace('h-12', 'h-4')} ${colorClass} bg-current rounded-full animate-pulse`}\n                style={{ animationDelay: `${i * 0.2}s` }}\n              />\n            ))}\n          </div>\n        );\n      \n      case 'wave':\n        return (\n          <div className=\"flex gap-1 items-end\">\n            {[0, 1, 2, 3, 4].map((i) => (\n              <div\n                key={i}\n                className={`${sizeClass.replace('w-6', 'w-1').replace('h-6', 'h-4').replace('w-8', 'w-1.5').replace('h-8', 'h-6').replace('w-12', 'w-2').replace('h-12', 'h-8')} ${colorClass} bg-current rounded-full animate-pulse`}\n                style={{ \n                  animationDelay: `${i * 0.1}s`,\n                  animationDuration: '1s'\n                }}\n              />\n            ))}\n          </div>\n        );\n      \n      case 'bars':\n        return (\n          <div className=\"flex gap-1\">\n            {[0, 1, 2, 3].map((i) => (\n              <div\n                key={i}\n                className={`${sizeClass.replace('w-6', 'w-1').replace('h-6', 'h-6').replace('w-8', 'w-1.5').replace('h-8', 'h-8').replace('w-12', 'w-2').replace('h-12', 'h-12')} ${colorClass} bg-current rounded animate-pulse`}\n                style={{ \n                  animationDelay: `${i * 0.15}s`,\n                  animationDuration: '1.2s'\n                }}\n              />\n            ))}\n          </div>\n        );\n      \n      case 'orbit':\n        return (\n          <div className={`relative ${sizeClass}`}>\n            <RefreshCw className={`w-full h-full ${colorClass} animate-spin`} />\n            <div className={`absolute inset-0 ${colorClass} bg-current rounded-full animate-ping opacity-75`} />\n          </div>\n        );\n      \n      case 'morph':\n        return (\n          <div className={`${sizeClass} ${colorClass} relative`}>\n            <Activity className=\"w-full h-full animate-pulse\" />\n            <Zap className=\"absolute inset-0 w-full h-full animate-ping opacity-50\" />\n          </div>\n        );\n      \n      default:\n        return <Loader2 className={`${sizeClass} ${colorClass} animate-spin`} />;\n    }\n  };\n\n  const loadingContent = (\n    <div className={`flex flex-col items-center justify-center gap-3 ${className}`}>\n      {renderLoadingIcon()}\n      {showText && text && (\n        <p className={`${getTextSizeClasses()} ${getColorClasses()} font-medium animate-pulse`}>\n          {text}\n        </p>\n      )}\n    </div>\n  );\n\n  if (fullScreen) {\n    return (\n      <div className=\"fixed inset-0 bg-black/80 backdrop-blur-sm flex items-center justify-center z-50\">\n        {loadingContent}\n      </div>\n    );\n  }\n\n  if (overlay) {\n    return (\n      <div className=\"absolute inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-40\">\n        {loadingContent}\n      </div>\n    );\n  }\n\n  return loadingContent;\n}\n\n// Predefined loading presets\nexport const LoadingPresets = {\n  // Game loading\n  gameLoading: (props?: Partial<LoadingEffectProps>) => (\n    <LoadingEffect\n      type=\"orbit\"\n      size=\"lg\"\n      color=\"primary\"\n      text=\"Loading Game...\"\n      fullScreen={true}\n      {...props}\n    />\n  ),\n\n  // Bet processing\n  betProcessing: (props?: Partial<LoadingEffectProps>) => (\n    <LoadingEffect\n      type=\"spinner\"\n      size=\"md\"\n      color=\"success\"\n      text=\"Processing Bet...\"\n      overlay={true}\n      {...props}\n    />\n  ),\n\n  // Authentication\n  authenticating: (props?: Partial<LoadingEffectProps>) => (\n    <LoadingEffect\n      type=\"pulse\"\n      size=\"lg\"\n      color=\"info\"\n      text=\"Authenticating...\"\n      fullScreen={true}\n      {...props}\n    />\n  ),\n\n  // Data fetching\n  dataFetching: (props?: Partial<LoadingEffectProps>) => (\n    <LoadingEffect\n      type=\"dots\"\n      size=\"sm\"\n      color=\"secondary\"\n      text=\"Loading...\"\n      {...props}\n    />\n  ),\n\n  // Network request\n  networkRequest: (props?: Partial<LoadingEffectProps>) => (\n    <LoadingEffect\n      type=\"wave\"\n      size=\"md\"\n      color=\"primary\"\n      text=\"Connecting...\"\n      {...props}\n    />\n  ),\n\n  // Simple spinner\n  simpleSpinner: (props?: Partial<LoadingEffectProps>) => (\n    <LoadingEffect\n      type=\"spinner\"\n      size=\"md\"\n      color=\"primary\"\n      showText={false}\n      {...props}\n    />\n  ),\n\n  // Minimal dots\n  minimalDots: (props?: Partial<LoadingEffectProps>) => (\n    <LoadingEffect\n      type=\"dots\"\n      size=\"sm\"\n      color=\"secondary\"\n      showText={false}\n      {...props}\n    />\n  )\n};\n\n// Hook for loading state management\nexport const useLoading = (initialState = false) => {\n  const [isLoading, setIsLoading] = React.useState(initialState);\n  const [loadingText, setLoadingText] = React.useState('Loading...');\n  const [loadingType, setLoadingType] = React.useState<'spinner' | 'pulse' | 'bounce' | 'dots' | 'wave' | 'bars' | 'orbit' | 'morph'>('spinner');\n\n  const startLoading = (text?: string, type?: typeof loadingType) => {\n    setIsLoading(true);\n    if (text) setLoadingText(text);\n    if (type) setLoadingType(type);\n  };\n\n  const stopLoading = () => {\n    setIsLoading(false);\n  };\n\n  const LoadingComponent = ({ overlay = false, fullScreen = false, className = '' }: { overlay?: boolean; fullScreen?: boolean; className?: string } = {}) => {\n    if (!isLoading) return null;\n    \n    return (\n      <LoadingEffect\n        type={loadingType}\n        text={loadingText}\n        overlay={overlay}\n        fullScreen={fullScreen}\n        className={className}\n      />\n    );\n  };\n\n  return {\n    isLoading,\n    setLoadingText,\n    setLoadingType,\n    startLoading,\n    stopLoading,\n    LoadingComponent\n  };\n};
